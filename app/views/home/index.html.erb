<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% if session[:user_id] %>
  <p>ログイン中: <%= User.find(session[:user_id]).name %></p>
  <%= button_to "ログアウト", logout_path, method: :delete %>
<% else %>
  <%= link_to "LINEでログイン", line_login_path %>
<% end %>